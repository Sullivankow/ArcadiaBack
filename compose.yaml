
services:
  backend:
    build:
      context: ../Zoo_ArcadiaBackend
    ports:
      - "8080:80"
    environment:
      - DATABASE_URL=mysql://arcadia:Arcadia123@db:3306/zoo_arcadia
      - MONGODB_URI=mongodb+srv://Arcadia:<db_password>@arcadia.qt1cv.mongodb.net/
    depends_on:
      - db

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: zoo_arcadia
      MYSQL_USER: arcadia
      MYSQL_PASSWORD: Arcadia123
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

  frontend:
    build:
      context: ../Zoo_ArcadiaFrontend  
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_BACKEND_URL=http://localhost:8080

  mongodb:
    image: mongo:latest

    environment:
      - MONGODB_INITDB_DATABASE=my_local_db
      - MONGODB_INITDB_ROOT_USERNAME=root
      - MONGODB_INITDB_ROOT_PASSWORD=rootpassword
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db:rw

volumes:
  db_data:
  mongodb_data:
